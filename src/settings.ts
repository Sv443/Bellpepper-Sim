export const settings = {
    game: {
        fps: 60,
    },
    player: {
        /** pixels per tick / frame */
        moveSpeed: 10,
    },
    debug: {
        /** draws all GameObjects' bounding boxes */
        drawBoundingBoxes: true,
    }
};
